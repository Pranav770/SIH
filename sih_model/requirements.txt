# SIH on-device inference dependencies
#
# Primary (edge) backend — ONNX Runtime:
onnxruntime>=1.18
numpy>=2.0
opencv-python>=4.8
pillow>=10.0

# Optional PyTorch fallback (large ~2GB; only if you want to run best.pt directly):
# ultralytics>=8.3
